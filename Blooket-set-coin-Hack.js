javascript:  //made by | NicolasT3ecoder! |
'require' = ("Blooket.com") = #customElements(KeyScript = ("")); // enter the key I gave you here!
   setInterval(handler, timehandler) = true.null;
 'arguments'.charCodeAt(importScripts).toExponential = selfWindow = true;
encodeURI.apply("arguments").apply.local = setTimeout(5); 
check: localFunction.import-File, (SetAmount('localFunction'(webkitURL))); 
  RegExp.bind-Caller(assign('class')); location.hostname = ('console, insert') = (localStorage).getItem=("coin, Set My Coins!"), null;
DOMMatrix.fromMatrix("bind, caller"(assignment).setTimeout(2)); 
  (local(importScripts).DOMMatrix(setAmount("setAmount" = true))); 
SaveLocalElement.apply(setAmount(900)); //set the amount of coins you want | made by: NicolasT3ecoder | 
// same properties
