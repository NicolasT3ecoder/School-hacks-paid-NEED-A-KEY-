--MainService 
 --Version 1.1
--Date: 3/23/23 

(LocalService) = (function()); 
  <div (local('SetService'(validate)));
local-value('return')=>(KeepService(update = return.update
)); 
   ('RunService'( return Service or return Service = Get:LocalService
   ));
UpdateVersion(Feature(repeat = "localService") and MainService = nil 
   <div = KeepNil('until'(5)));
Local(Service("SetService"(repeat)));
