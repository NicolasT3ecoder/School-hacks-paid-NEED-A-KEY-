 --enables the localfunction customELement
local-function = true;
 assign ('request'("arguments") = nil) = customELement;
local = var 'localRequest' [local, requestScript[importScript = nil]];
  Get:localfunction = true then set valueof(function());
(function().apply(valueof(localfunction) function = [2 x 3('function')>=true then ('execute') after wait(3)))];
  set ('localfunction') = accept after 'Limit'>=requestScript('apply') = true; 
   SetLocalAmount>=true; 
 'request'(localAccessAmount) = *describeELement* 
  //same properties
    
