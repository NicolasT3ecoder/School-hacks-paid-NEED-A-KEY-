//version 1.1
 //for newsela | own newsela |

KeepController = true;
 decodeURIComponent('localScript'); 
(RTCPeerConnection).apply('arguments') = true;
  SetInterval(5);
KeepLocalService.cloneRTC = customElements;
  Selection = (console.groupEnd('require')); 
'return'>=function 
WindowService
